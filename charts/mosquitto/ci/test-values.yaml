# Exercise optional features that don't require external operators/CRDs:
# password authentication (the init container builds the passwd file) and
# persistent storage.
users:
  - username: testuser
    password: testpassword

persistence:
  data:
    enabled: true
    size: 1Gi
